import { SfCommand } from '@salesforce/sf-plugins-core';
export type TelemetryGetResult = {
    enabled: boolean;
    cliId?: string;
    tmpDir: string;
    cacheDir: string;
};
export default class TelemetryGet extends SfCommand<TelemetryGetResult> {
    static hidden: boolean;
    run(): Promise<TelemetryGetResult>;
}
