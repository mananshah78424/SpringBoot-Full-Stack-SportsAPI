import { SfError } from '@salesforce/core';
/**
 * convenience method for doing something SfError doesn't allow
 * (explicitly setting the error name)
 */
export declare const setErrorName: (err: SfError, name: string) => SfError;
