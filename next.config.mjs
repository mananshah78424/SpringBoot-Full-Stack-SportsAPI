/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.api-sports.io",
      "media.formula1.com",
      "resources.premierleague.com",
    ],
  },
};

export default nextConfig;
